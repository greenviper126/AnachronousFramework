--!strict

local Utility = script

local list = {
	Attributes = require(Utility.Attributes),
	Properties = require(Utility.Properties),
	Tags = require(Utility.Tags),
	Weight = require(Utility.Weight),
	PartTween = require(Utility.PartTween),
}

return list
