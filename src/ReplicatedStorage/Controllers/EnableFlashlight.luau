--!strict

local ReplicatedStorage = game:GetService("ReplicatedStorage")

local FlashlightUtility = require(ReplicatedStorage.ClientCommon.FlashlightUtility)

FlashlightUtility.SetCanActivate(true)

return {}
